package src

import fmt "core:fmt"

INTERACTION_tutorial_npc_event :: proc() {
    fmt.printfln("interacted with tutorial")
}
INTERACTION_fishemans_npc_event :: proc() {
    fmt.printfln("interacted with fishemans")
}